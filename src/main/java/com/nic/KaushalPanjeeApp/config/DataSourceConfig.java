package com.nic.KaushalPanjeeApp.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class DataSourceConfig {

	/*
	 * @Bean(name = "externalDataSource") public DataSource externalDataSource() {
	 * BasicDataSource dataSource = new BasicDataSource();
	 * dataSource.setUrl("jdbc:postgresql://localhost:5432/ddugky16_04_2025");
	 * dataSource.setUsername("postgres"); dataSource.setPassword("postgres");
	 * dataSource.setDriverClassName("org.postgresql.Driver"); return dataSource; }
	 * 
	 * @Bean(name = "externalJdbcTemplate") public JdbcTemplate
	 * externalJdbcTemplate(DataSource externalDataSource) { return new
	 * JdbcTemplate(externalDataSource); }
	 */
}
